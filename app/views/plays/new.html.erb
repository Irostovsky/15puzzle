<div id='notice'><%= flash[:notice] %></div>
<div id='error'><%= flash[:error] %></div>
<%= link_to "Start new game", new_plays_path %>
<table>
  <% @puzzle.matrix.to_a.map do |row |%>
    <tr>
      <% row.map do |item|  %>
        <td class = <%= 'clickable' if clickable?(item) %> >
          <% if clickable? item %>
            <%= link_to item, edit_plays_path(:p => item) %>
          <% else %>
            <%= item %>
          <% end %>
        </td>
      <% end %>
    </tr>
  <% end %>
</table>
